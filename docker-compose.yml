services:
  redis:
    image: redis:7-alpine
    container_name: ghost-racer-redis
    ports:
      - "6379:6379"
    volumes:
      - ghost-racer-redis:/data
    command: ["redis-server", "--appendonly", "yes"]

volumes:
  ghost-racer-redis:
